[pytest]
testpaths = tests
addopts = --self-contained-html -v --cov=. --cov-report=term-missing --cov-report=html:coverage-report
markers =
    unit: unit tests for individual functions
    integration: integration tests for API endpoints
